#include "controller.hpp"

void Controller::execute_command(
    const std::string& command_input, const std::vector<std::string>& args) {
        
}

void Controller::change_view(std::unique_ptr<View> newView) {
    
}
